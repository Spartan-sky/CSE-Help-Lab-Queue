export class Test {
    id: number;
    name: string;
    course: string;
    proff: string; 
    constructor (id:number, name: string, course: string, proff: string) {
        this.id = id;
        this.name = name;
        this.course = course;
        this.proff = proff;
    }
}